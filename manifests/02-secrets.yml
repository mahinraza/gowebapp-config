apiVersion: v1
kind: Secret
metadata:
  name: gowebapp-secrets
  namespace: gowebapp
type: Opaque
stringData:
  # MySQL root password — used by both the MySQL StatefulSet and the app config
  mysql-root-password: "Ch4ngeMe_Str0ng!"
 
  # MySQL app user password
  mysql-app-password: "AppP4ss_Ch4ngeMe!"
 
  # Session secret key — must be exactly as the app reads it from config.json.
  # Keep this consistent across all pod replicas.
  session-secret-key: "@r4B?EThaSEh_drudR7P_hub=s#s2Pah"